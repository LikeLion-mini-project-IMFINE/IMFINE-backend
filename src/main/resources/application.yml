server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://imfine-db.cnsmso222f2s.ap-northeast-2.rds.amazonaws.com:3306/imfine-db?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: likelion
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect